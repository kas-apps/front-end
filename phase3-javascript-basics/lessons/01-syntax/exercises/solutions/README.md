# Lesson 1: JavaScript 基礎文法 - 解答例と解説 💡

演習問題の解答例と、それぞれの解説を見ていこう！

---

## 基礎編

### 問題 1-1: 変数の宣言と出力 - 解答例

**解答例**: [01-01.html](01-01.html)

```javascript
// 変数を宣言して値を代入
const myName = "太郎";
const myAge = 25;
const myHometown = "東京";

// コンソールに出力
console.log(myName);
console.log(myAge);
console.log(myHometown);
```

**解説**：

- `const` を使って変数を宣言している（値が変わらないため）
- それぞれの変数に文字列（名前、出身地）と数値（年齢）を代入
- `console.log()` で一つずつ出力している

**ポイント**：

- 名前と出身地は文字列なので、クォート `"` で囲む
- 年齢は数値なので、クォートは不要
- 変数名は意味が分かりやすい名前にする（`x`, `a` などは避ける）

---

### 問題 1-2: データ型の確認 - 解答例

**解答例**: [01-02.html](01-02.html)

```javascript
// さまざまなデータ型の変数を宣言
const num = 100;
const str = "100";
const bool = true;
const emptyValue = null;
let notAssigned; // 値を代入しない

// 値と型を出力
console.log(`値: ${num}, 型: ${typeof num}`);
console.log(`値: ${str}, 型: ${typeof str}`);
console.log(`値: ${bool}, 型: ${typeof bool}`);
console.log(`値: ${emptyValue}, 型: ${typeof emptyValue}`);
console.log(`値: ${notAssigned}, 型: ${typeof notAssigned}`);
```

**解説**：

- 数値、文字列、真偽値、null、undefined の 5 つのデータ型を扱っている
- `typeof` 演算子で変数の型を確認できる
- テンプレートリテラルを使って、値と型を一緒に出力している

**ポイント**：

- `100` と `"100"` は見た目は同じだけど、型が違う（number と string）
- `null` の型は `object` になる（これは JavaScript の歴史的なバグ）
- 値を代入しない変数は自動的に `undefined` になる

---

### 問題 1-3: 基本的な計算 - 解答例

**解答例**: [01-03.html](01-03.html)

```javascript
// 変数を宣言
const price = 800;
const quantity = 5;
const taxRate = 0.1;

// 計算
const subtotal = price * quantity;
const tax = subtotal * taxRate;
const total = subtotal + tax;

// 結果を出力
console.log(`商品価格: ${price}円`);
console.log(`購入数量: ${quantity}個`);
console.log(`小計: ${subtotal}円`);
console.log(`消費税: ${tax}円`);
console.log(`合計金額: ${total}円`);
```

**解説**：

- 段階的に計算している（小計 → 消費税 → 合計）
- それぞれの計算結果を変数に保存して、後で使えるようにしている
- テンプレートリテラルで単位（円、個）を付けて出力している

**ポイント**：

- 計算の順序が大事：まず小計を計算してから、消費税を計算する
- 税率は 0.1（10%）で表現する
- 途中結果も変数に入れると、後で確認しやすい

---

## 応用編

### 問題 1-4: テンプレートリテラルで自己紹介 - 解答例

**解答例**: [01-04.html](01-04.html)

```javascript
// 変数を宣言
const name = "太郎";
const age = 25;
const hobby = "読書";

// テンプレートリテラルで自己紹介文を作成
const introduction = `こんにちは！私の名前は${name}です。
年齢は${age}歳で、趣味は${hobby}です。
よろしくお願いします！`;

// 出力
console.log(introduction);
```

**解説**：

- バッククォート `` ` `` を使ったテンプレートリテラルで複数行の文章を作成
- `${}` の中に変数を埋め込んでいる
- 改行もそのまま反映される

**ポイント**：

- バッククォート `` ` `` を使うと、改行が簡単にできる
- `+` で文字列を連結するより、読みやすくて書きやすい
- `${}` の中には変数だけでなく、式（計算など）も入れられる

---

### 問題 1-5: 比較演算子の活用 - 解答例

**解答例**: [01-05.html](01-05.html)

```javascript
// 点数を代入
const score = 85;

// 比較演算子で判定
const isHighScore = score >= 80;
const isLowScore = score < 60;
const isPerfect = score === 100;

// 結果を出力
console.log(`点数: ${score}点`);
console.log(`80点以上: ${isHighScore}`);
console.log(`60点未満: ${isLowScore}`);
console.log(`満点: ${isPerfect}`);
```

**解説**：

- 比較演算子（`>=`, `<`, `===`）を使って判定している
- 判定結果（true または false）を変数に保存している
- テンプレートリテラルで分かりやすく出力している

**ポイント**：

- `>=` は「以上」、`<` は「未満」、`===` は「等しい」
- 比較演算子の結果は真偽値（boolean）になる
- `===` を使って厳密に比較する（`==` は使わない）

---

### 問題 1-6: 複数の演算子を組み合わせる - 解答例

**解答例**: [01-06.html](01-06.html)

```javascript
// 変数を宣言
const a = 10;
const b = 20;

// 算術演算子で計算
const sum = a + b;
const difference = a - b;
const product = a * b;
const quotient = a / b;
const remainder = a % b;

// 比較演算子で判定
const isLess = a < b;

// 論理演算子で判定
const bothGreaterThan10 = a >= 10 && b >= 10;

// 結果を出力
console.log(`a = ${a}, b = ${b}`);
console.log(`和: ${sum}`);
console.log(`差: ${difference}`);
console.log(`積: ${product}`);
console.log(`商: ${quotient}`);
console.log(`余り: ${remainder}`);
console.log(`aはbより小さい: ${isLess}`);
console.log(`両方とも10以上: ${bothGreaterThan10}`);
```

**解説**：

- 算術演算子（+, -, \*, /, %）で計算している
- 比較演算子（<）で大小を判定している
- 論理演算子（&&）で複数の条件を組み合わせている
- すべての結果を変数に保存して、分かりやすく出力している

**ポイント**：

- `%` は余りを求める演算子（10 ÷ 20 = 0 余り 10）
- `&&` は「かつ」の意味（両方 true なら true）
- 複数の演算子を組み合わせると、複雑な判定ができる

---

## チャレンジ編

### 問題 1-7: 買い物の明細書を作る - 解答例

**解答例**: [01-07.html](01-07.html)

```javascript
// 商品情報
const item1Name = "りんご";
const item1Price = 100;
const item1Quantity = 3;

const item2Name = "バナナ";
const item2Price = 100;
const item2Quantity = 2;

const item3Name = "オレンジ";
const item3Price = 100;
const item3Quantity = 5;

// 各商品の小計を計算
const item1Total = item1Price * item1Quantity;
const item2Total = item2Price * item2Quantity;
const item3Total = item3Price * item3Quantity;

// 合計金額を計算
const subtotal = item1Total + item2Total + item3Total;
const taxRate = 0.1;
const tax = subtotal * taxRate;
const total = subtotal + tax;

// 明細を出力
console.log("=== 買い物明細 ===");
console.log(`${item1Name} × ${item1Quantity}個: ${item1Total}円`);
console.log(`${item2Name} × ${item2Quantity}個: ${item2Total}円`);
console.log(`${item3Name} × ${item3Quantity}個: ${item3Total}円`);
console.log("---");
console.log(`小計: ${subtotal}円`);
console.log(`消費税(10%): ${tax}円`);
console.log("---");
console.log(`合計金額: ${total}円`);
```

**解説**：

- 3 つの商品それぞれに、名前・価格・数量の変数を用意
- 各商品の小計を計算してから、全体の合計を計算
- 消費税を計算して、最終的な支払い金額を出す
- テンプレートリテラルと改行を使って、見やすい明細を作成

**ポイント**：

- 変数名を分かりやすくする（`item1Name`, `item1Price` など）
- 計算を段階的に行う（各商品の小計 → 全体の小計 → 消費税 → 合計）
- 出力の見た目を整えると、より実践的になる

**別解（もっと実践的な方法）**：

この問題は、配列とオブジェクトを使うともっと簡潔に書けるよ！（Lesson 4 で学ぶ）

```javascript
// Lesson 4 を学んだ後の書き方（参考）
const items = [
  { name: "りんご", price: 100, quantity: 3 },
  { name: "バナナ", price: 100, quantity: 2 },
  { name: "オレンジ", price: 100, quantity: 5 },
];

// ループで処理（もっとスマート！）
```

---

## よくある間違いと修正方法

### 間違い 1: クォートの種類を間違える

**NG**：

```javascript
const message = "こんにちは、${name}さん"; // ダブルクォート
console.log(message); // → こんにちは、${name}さん（展開されない）
```

**OK**：

```javascript
const message = `こんにちは、${name}さん`; // バッククォート
console.log(message); // → こんにちは、太郎さん
```

---

### 間違い 2: 数値を文字列で宣言してしまう

**NG**：

```javascript
const price = "1000"; // 文字列
const quantity = "3"; // 文字列
const total = price * quantity; // → 3000 (自動変換されるが非推奨)
```

**OK**：

```javascript
const price = 1000; // 数値
const quantity = 3; // 数値
const total = price * quantity; // → 3000
```

---

### 間違い 3: const で宣言した変数を変更しようとする

**NG**：

```javascript
const age = 25;
age = 26; // ❌ エラー
```

**OK**：

```javascript
let age = 25; // let に変更
age = 26; // ✅ OK
```

---

### 間違い 4: == を使ってしまう

**NG**：

```javascript
if (age == 20) {
  // あいまい比較
}
```

**OK**：

```javascript
if (age === 20) {
  // 厳密比較
}
```

---

## まとめ

### このレッスンで使った重要なポイント

1. **変数宣言**：基本は `const`、変更が必要なら `let`
2. **データ型**：数値、文字列、真偽値を正しく使い分ける
3. **演算子**：算術、比較、論理演算子で計算と判定
4. **テンプレートリテラル**：バッククォート `` ` `` と `${}` で見やすく
5. **console.log()**：動作確認の必須ツール

---

### 次のステップ

基礎文法の演習、お疲れ様！

次は**Lesson 2: 条件分岐とループ**で、「もし〇〇なら××する」という処理や、繰り返し処理を学ぼう！

👉 [Lesson 2 へ進む](../../02-control-flow/README.md)

---

**Let's vibe and code!** 🎉
