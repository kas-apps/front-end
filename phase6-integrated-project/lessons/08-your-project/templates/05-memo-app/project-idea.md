# プロジェクトアイデア：メモアプリ 📝

---

## プロジェクト概要

### 何を作るか

メモを作成・編集・整理できるシンプルなWebアプリケーション

### 誰が使うか

- メモを取るのが好きな人
- アイデアをメモしたい人
- メモを整理したい人

### どんな問題を解決するか

- メモが散らばっていて、見つからない
- タグやカテゴリで整理したい
- 検索して素早く見つけたい
- 重要なメモをピン留めしたい

---

## ユーザーストーリー例

### US-001：メモの作成

```text
ユーザーとして、
メモを作成したい、
なぜならアイデアや情報を記録しておきたいから
```

### US-002：メモの整理

```text
ユーザーとして、
メモにタグを付けたい、
なぜなら関連するメモをまとめて管理したいから
```

### US-003：重要なメモのピン留め

```text
ユーザーとして、
重要なメモをピン留めしたい、
なぜなら常に上部に表示させたいから
```

---

## 機能要件の例

### 必須機能（Must Have）

#### F-001：メモの追加

- タイトル（必須、最大100文字）
- 内容（必須、最大5000文字）
- タグ（任意、カンマ区切り）
- カラー（任意、カードの色）

#### F-002：メモの一覧表示

- カード形式で表示
- ピン留めメモは上部に表示
- タイトル、内容の一部（3行まで）、タグ、更新日を表示

#### F-003：メモの削除

- 削除ボタンをクリック
- 確認ダイアログ表示

#### F-004：統計表示

- 全メモ数
- タグ別のメモ数

### 拡張機能（Should Have）

#### F-101：メモの編集

#### F-102：ピン留め機能

#### F-103：タグフィルター

#### F-104：作成日・更新日でソート

#### F-105：タイトル・内容で検索

#### F-106：カラー選択（カード背景色）

---

## データ構造の例

### メモオブジェクト

```javascript
{
  id: number,
  title: string,
  content: string,
  tags: string[],                                 // ['仕事', 'アイデア']
  color: string | null,                           // 'yellow' | 'blue' | 'green' | null
  isPinned: boolean,                              // ピン留めフラグ
  createdAt: string,
  updatedAt: string
}
```

### メモ配列の例

```javascript
const memos = [
  {
    id: 1638360000000,
    title: "プロジェクトのアイデア",
    content: "Phase 6でレシピ管理アプリを作ろうと思う。\n材料と手順をリストで管理できるようにする。",
    tags: ["仕事", "アイデア"],
    color: "yellow",
    isPinned: true,
    createdAt: "2025-12-01T10:00:00.000Z",
    updatedAt: "2025-12-01T14:30:00.000Z"
  },
  // ...他のメモ
];
```

---

## 画面構成のアイデア

### メモカード

```text
┌──────────────────────────────────┐
│ 📌 プロジェクトのアイデア            │
├──────────────────────────────────┤
│ Phase 6でレシピ管理アプリを作ろう　　 │
│ と思う。材料と手順をリストで...　     │
├──────────────────────────────────┤
│ #仕事 #アイデア                    │
│ 更新: 2025-12-01 14:30　          │
├──────────────────────────────────┤
│ [編集] [ピン解除] [削除]　          │
└──────────────────────────────────┘
```

### カラー選択

```text
┌──────────────────────────────────┐
│  カラー:                          │
│  ⬜ 白 🟨 黄 🟦 青 🟩 緑 🟥 赤　　   │
└──────────────────────────────────┘
```

---

## TODOアプリとの違い

| 項目 | TODOアプリ | メモアプリ |
|------|-----------|-----------|
| メインデータ | タスク | メモ |
| 内容 | 短い説明 | 長い本文 |
| タグ | なし | 複数タグ可能 |
| ピン留め | なし | あり |
| カラー | 優先度バッジ | カード背景色 |

---

## 実装のヒント

### タグの入力と保存

```javascript
// タグ入力欄の値を配列に変換
function parseTags(tagsInput) {
  return tagsInput
    .split(',')
    .map(tag => tag.trim())
    .filter(tag => tag.length > 0);
}

// 使用例
parseTags('仕事, アイデア, Phase 6');
// → ['仕事', 'アイデア', 'Phase 6']
```

### タグの表示

```javascript
function renderTags(tags) {
  return tags
    .map(tag => `<span class="tag">#${tag}</span>`)
    .join(' ');
}

// HTML
<div class="tags">
  ${renderTags(memo.tags)}
</div>
```

### ピン留めメモの並び替え

```javascript
function getSortedMemos() {
  return [...memos].sort((a, b) => {
    // ピン留めメモを上部に表示
    if (a.isPinned && !b.isPinned) return -1;
    if (!a.isPinned && b.isPinned) return 1;

    // ピン留め状態が同じ場合は、更新日でソート
    return new Date(b.updatedAt) - new Date(a.updatedAt);
  });
}
```

### カラークラスの適用

```javascript
function getColorClass(color) {
  const colorClasses = {
    yellow: 'bg-yellow-100',
    blue: 'bg-blue-100',
    green: 'bg-green-100',
    red: 'bg-red-100'
  };
  return colorClasses[color] || 'bg-white';
}

// HTML
<div class="card ${getColorClass(memo.color)}">
  <!-- メモの内容 -->
</div>
```

### 内容の省略表示

```javascript
function truncateContent(content, maxLines = 3) {
  const lines = content.split('\n');
  if (lines.length <= maxLines) {
    return content;
  }

  return lines.slice(0, maxLines).join('\n') + '...';
}

// 使用例
truncateContent('行1\n行2\n行3\n行4\n行5', 3);
// → '行1\n行2\n行3...'
```

### ピン留めの切り替え

```javascript
function togglePin(memoId) {
  const memo = memos.find(m => m.id === memoId);
  if (!memo) return;

  memo.isPinned = !memo.isPinned;
  memo.updatedAt = new Date().toISOString();

  saveToLocalStorage();
  renderMemos();

  const message = memo.isPinned ? 'メモをピン留めしました' : 'ピン留めを解除しました';
  showToast(message, 'success');
}
```

### タグでフィルター

```javascript
function filterByTag(tag) {
  return memos.filter(memo =>
    memo.tags.includes(tag)
  );
}

// タグクラウドの作成
function getAllTags() {
  const allTags = new Set();
  memos.forEach(memo => {
    memo.tags.forEach(tag => allTags.add(tag));
  });
  return Array.from(allTags).sort();
}
```

---

## AIへの質問例

```text
「メモアプリを作りたいです。

【背景】
- メモをたくさん取るが、整理できていない
- タグで分類して、素早く見つけたい
- 重要なメモは常に上部に表示したい

【質問】
1. メモアプリに必要な機能は何ですか？
2. タグは配列と文字列、どちらで保存すべきですか？
3. ピン留めメモを上部に表示するソート方法は？

具体的なアドバイスをお願いします！」
```

---

**Let's vibe and code!** 🎉

メモアプリで、あなたのアイデアを整理しよう！ 📝
